{
  "name": "clearstream",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Detect HLS streams on any page and play them in a clean, ad-free player.",
  "license": "MIT",
  "scripts": {
    "dev": "wxt",
    "dev:firefox": "wxt -b firefox",
    "build": "wxt build",
    "build:firefox": "wxt build -b firefox",
    "build:edge": "wxt build -b edge",
    "zip": "wxt zip",
    "zip:firefox": "wxt zip -b firefox",
    "icons": "node scripts/gen-icons.mjs",
    "compile": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "size": "node scripts/check-bundle-size.mjs",
    "audit:code": "node scripts/audit-no-remote-code.mjs",
    "lint": "eslint .",
    "lint:ext": "web-ext lint --source-dir .output/firefox-mv3 --no-config-discovery",
    "check": "pnpm compile && pnpm lint && pnpm test && pnpm build && pnpm build:firefox && pnpm build:edge && pnpm size && pnpm audit:code && pnpm lint:ext",
    "verify": "wxt build && node scripts/verify.mjs",
    "verify:firefox": "wxt build -b firefox && node scripts/verify-firefox.mjs",
    "postinstall": "wxt prepare"
  },
  "dependencies": {
    "hls.js": "1.6.16",
    "media-chrome": "4.19.2",
    "media-tracks": "0.3.5"
  },
  "devDependencies": {
    "@wxt-dev/auto-icons": "^1.1.1",
    "eslint": "^10.5.0",
    "eslint-plugin-no-unsanitized": "^4.1.5",
    "playwright": "^1.60.0",
    "selenium-webdriver": "^4.44.0",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.61.0",
    "vitest": "^4.1.8",
    "web-ext": "^10.4.0",
    "wxt": "^0.20.0"
  }
}
